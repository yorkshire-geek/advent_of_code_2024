import { describe, expect, it } from "bun:test";
import { GameGrid } from "./day_06";

describe('Day 06', () => {
  describe('Setup the Grid', () => {
    // it('should parse grid correctly', async () => {
    //   const data = await Bun.file(import.meta.dir + "/input.test.txt").text();
    //   const gameGrid = GameGrid.factory(data);

    //   expect(gameGrid.getGrid()[0][0]).toBe('.');
    //   expect(gameGrid.getGrid()[0][4]).toBe('#');
    //   expect(gameGrid.getGrid()[6][4]).toBe('^');

    //   console.log("WIDTH: ", gameGrid.WIDTH);
    //   console.log("HEIGHT: ", gameGrid.HEIGHT);

    //   //expect(gameGrid.getLocation()).toEqual([6,4]);
    // });
  });

  describe('Part Two', () => {
    // it('should ', () => {
    //   // Test implementation
    // });
  });
});
